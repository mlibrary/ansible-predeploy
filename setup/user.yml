---

user_group: app_name
user_name: app_name
user_home:
  global: app_name
  output: File.join("/", "var", "local", app_name)
user_uid: app_user_uid
user_gid: app_user_gid
user_ssh_key:
  default: "{{ lookup('file', 'files/ssh/id_rsa-fauxpaas.pub') }}"

- hosts: all
  become: true
  tasks:
    - name: create and modify user
      user:
        name: "{{usernames}}"
        password: "{% verbatim %}{{ password }}{% endverbatim %}"
        shell: /bin/bash
        group: "{{group}}"
        groups: "{{additional_groups}}"
        append: yes
        uid: "{{uid}}"